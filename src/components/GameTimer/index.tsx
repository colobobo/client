import React, { FC } from "react";

// style
import "./index.scss";

const GameTimer: FC = () => {
  // states

  //handles

  // return

  return (
    <div className="timer">
      <div className="timer__container">
        <div className="timer__progress"></div>
      </div>
    </div>
  );
};

export default GameTimer;
